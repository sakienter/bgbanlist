name: Add trinket pool restrictions
on:
  push:
    paths:
      - scripts/add_trinket_pool_restrictions.py
      - .github/workflows/add-trinket-pool-restrictions.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/add_trinket_pool_restrictions.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Add trinket pool restrictions"
          git push
